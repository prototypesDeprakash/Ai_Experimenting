Trying to create a ai that can access tools , something like a agent , so far its just a generative ai , 
i need to figure out a way to make the agent access the tools and use them dynamically .

so far i made a rough version that opens calcuator tool and prints hello world tool , the ai can access these two tools when the user asks to 
i also created a proper architecture where i created separate py files for each tools and split the whole project into a manageble part.
